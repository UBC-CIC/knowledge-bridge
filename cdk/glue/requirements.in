# Direct dependencies for the Glue Python-shell job.
# boto3/botocore are intentionally omitted — Glue provides them at runtime.
#
# To regenerate requirements.txt after changing this file, run pip-compile
# inside a Python 3.9 environment (matching the Glue runtime):
#   pip install pip-tools
#   pip-compile requirements.in
azure-identity>=1.21.0
msgraph-sdk>=1.27.0
psycopg2-binary>=2.9.9
httpx>=0.27.0
requests>=2.32.3
